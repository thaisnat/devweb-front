import sendBotton from '../botton-send';
import textField from '../text-field';

const searchbox = props => {
    return(
        <div className="search-box">
                <sendBotton onClick></sendBotton>
         </div>
        );
};